@model EpiserverSite.Models.ViewModels.FAQListingPageViewModel
<section class="faq-section">

    @if (Model.FAQPages.Any())
    {
        <dl class="faq__items">
            @foreach (var FAQPage in Model.FAQPages)
            {
                {
                    Html.RenderPartial("PagePartials/FAQPage", FAQPage);
                }
            }
        </dl>
    }

</section>